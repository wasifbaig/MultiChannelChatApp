.online
	h4.inner-padding User Status
	.people
		-users_list.forEach(function(user) {
			a(class=user.status, href='#', data-username=user.username, data-provider=user.provider)
				b
				= user.username
		-});
	// .people 
// .online 
